pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id("org.jetbrains.intellij.platform") version "2.10.5"
    }
}

rootProject.name = "branch-warner"
